# Dotfiles

```
Usage: setup <OPTIONS>

Options:
 -n <value> Full name
 -e <value> Email address
 -f         Force, always overwrite existing files
 -h         This help screen
 -s <value> Select which sections to run, omit for all. Can be added multiple times. Valid options: git
```
